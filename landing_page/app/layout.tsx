import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "StackTap - Simple Arcade Stacking Game",
  description: "Tap to stack blocks higher and higher. Build the tallest tower possible in this addictive arcade game.",
  keywords: "arcade game, stacking game, mobile game, casual game",
  authors: [{ name: "StackTap" }],
  icons: {
    icon: "/logo.svg",
    favicon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
